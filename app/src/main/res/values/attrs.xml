<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2010 The Android Open Source Project

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
     See the License for the specific language governing permissions and
     limitations under the License.
-->

<resources>

    <declare-styleable name="LatinKeyboardBaseView">
        <!-- Default KeyboardView style. -->
        <attr name="keyboardViewStyle" format="reference" />

        <!-- Image for the key. This image needs to be a StateListDrawable, with the following
             possible states: normal, pressed, checkable, checkable+pressed, checkable+checked,
             checkable+checked+pressed. -->
        <attr name="keyBackground" format="reference" />

        <!-- Size of the text for character keys. -->
        <attr name="keyTextSize" format="dimension" />

        <!-- Size of the text for custom keys with some text and no icon. -->
        <attr name="labelTextSize" format="dimension" />

        <!-- Color to use for the label in a key. -->
        <attr name="keyTextColor" format="color" />

        <!-- Color to use for key hint labels. -->
        <attr name="keyHintColor" format="color" />

        <!-- Color to use for cursor key labels. -->
        <attr name="keyCursorColor" format="color" />

        <!-- Do symbols need to be inverted (for light-background themes)? -->
        <attr name="invertSymbols" format="boolean" />

	<!--  Do symbols need to be recolored? -->
	<attr name="recolorSymbols" format="boolean" />

	<!--  Transparent key background? 0=fully transparent, 255=opaque (fast) -->
	<attr name="backgroundAlpha" format="integer" />
	
        <!-- Layout resource for key press feedback.-->
        <attr name="keyPreviewLayout" format="reference" />

        <!-- Vertical offset of the key press feedback from the key. -->
        <attr name="keyPreviewOffset" format="dimension" />

        <!-- Height of the key press feedback popup. -->
        <attr name="keyPreviewHeight" format="dimension" />

        <!-- Hysteresis distance for key debouncing -->
        <attr name="keyHysteresisDistance" format="dimension" />

        <!-- Amount to offset the touch Y coordinate by, for bias correction. -->
        <attr name="verticalCorrection" format="dimension" />

        <!-- Layout resource for popup keyboards. -->
        <attr name="popupLayout" format="reference" />

        <attr name="shadowColor" format="color" />
        <attr name="shadowRadius" format="float" />
        <attr name="backgroundDimAmount" format="float" />

        <attr name="keyTextStyle">
            <flag name="normal" value="0" />
            <flag name="bold" value="1" />
            <flag name="italic" value="2" />
        </attr>

        <attr name="symbolColorScheme">
            <flag name="white" value="0" />
            <flag name="black" value="1" />
        </attr>

        <!-- ============================================== -->
        <!-- TOKYO NIGHT SEMANTIC THEME ATTRIBUTES          -->
        <!-- ============================================== -->
        <!-- These attributes allow the layout to ask for a "Role" -->
        <!-- rather than a specific color. The active Theme    -->
        <!-- provides the specific hex value.                  -->
        
        <!-- The global background canvas of the keyboard -->
        <attr name="kbdColorBase" format="color" />
        
        <!-- The key background for standard alpha keys (A-Z) -->
        <attr name="kbdColorAlpha" format="color" />
        
        <!-- The key background for modifier keys (Shift, Ctrl) -->
        <attr name="kbdColorMod" format="color" />
        
        <!-- The key background when PRESSED -->
        <attr name="kbdColorHighlight" format="color" />
        
        <!-- The main label text color -->
        <attr name="kbdColorText" format="color" />
        
        <!-- The accent color for special symbols (Arrows) -->
        <attr name="kbdColorAccent" format="color" />
        
        <!-- The color for active/sticky modifier indicators (Ctrl, Alt) -->
        <attr name="kbdColorActive" format="color" />
        
        <!-- The border/stroke color for popups -->
        <attr name="kbdColorPopup" format="color" />

    </declare-styleable>

</resources>
